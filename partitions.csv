# X3 OSINT OS partition table — Xteink X3 (ESP32-C3, 16 MB flash)
# Matches the CrossPoint-family layout so the CrossPoint web flasher and the
# device's expected layout line up (app0 at 0x10000, LittleFS "spiffs" at
# 0xc90000). LittleFS mounts the "spiffs"-labelled partition by name.
# Name,     Type, SubType,  Offset,   Size,     Flags
nvs,        data, nvs,      0x9000,   0x5000,
otadata,    data, ota,      0xe000,   0x2000,
app0,       app,  ota_0,    0x10000,  0x640000,
app1,       app,  ota_1,    0x650000, 0x640000,
spiffs,     data, spiffs,   0xc90000, 0x360000,
coredump,   data, coredump, 0xFF0000, 0x10000,
